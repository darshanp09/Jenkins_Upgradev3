pipeline {
    agent any
    stages {
        stage('Init') {
            steps {
                echo "Hello this is darshan from levelup 360"
                echo "We are starting testing"
            }
        }
        stage('Build') {
            steps {
                echo "Building same maven project"
            }
        }
        stage('Deploy') {
            steps {
                echo "Deploying the build"
            }
        }
    }
}